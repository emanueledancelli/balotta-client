import React from "react";
import { LoaderContainer, LoaderText } from "./style";

export const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderText>Balotta</LoaderText>
    </LoaderContainer>
  );
};
